# Nodes examples 1

This example illustrates an example of a project starting with a peppy config that serves as a parent & launcher for all other child nodes.

It uses dependencies and services to show an example of how to connect the nodes together.
